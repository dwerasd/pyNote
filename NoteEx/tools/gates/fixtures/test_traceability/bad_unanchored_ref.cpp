// 결함 표본: 앞줄에 경로가 없는 `::이름` 표기.
//
// 게이트가 받아들이는 "앞 경로를 잇는 표기"의 실패면이다. 이식 트리에는 앞줄에 경로를
// 적고 다음 줄에서 `::이름` 으로 잇는 자리가 있는데, 그 앞줄을 지우거나 시험 사이로
// 옮기면 이 모양이 남는다. 어느 파일의 시험인지 아무도 알 수 없다.

#include <catch2/catch_test_macros.hpp>

// 대응 원본: 저장소 계층의 검증 두 갈래
// 와 ::test_alpha (repositories.py:722~737).
TEST_CASE("경로 없이 이름만 이었다", "[fixture]")
{
	REQUIRE(true);
}
